import { AnimatePresence, motion } from 'framer-motion';
import { X } from './Icons';
export function Modal({open,onClose,title,children,className='' ,sheet=false}:{open:boolean;onClose:()=>void;title:string;children:React.ReactNode;className?:string;sheet?:boolean}){return <AnimatePresence>{open&&<><motion.button className="modal-backdrop" aria-label="Close dialog" onClick={onClose} initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}/><motion.div className={`modal ${sheet?'modal-sheet':''} ${className}`} role="dialog" aria-modal="true" aria-label={title} initial={{opacity:0,y:sheet?40:18,scale:.98}} animate={{opacity:1,y:0,scale:1}} exit={{opacity:0,y:sheet?32:10,scale:.98}} transition={{type:'spring',stiffness:380,damping:30}}><div className="modal-top"><div><div className="eyebrow">{sheet?'QUICK ACTION':'CALMSPACE'}</div><h2>{title}</h2></div><button className="icon-btn" onClick={onClose} aria-label="Close"><X size={18}/></button></div>{children}</motion.div></>}</AnimatePresence>}
