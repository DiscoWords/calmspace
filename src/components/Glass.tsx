import React from 'react';
export function Glass({children,className='',level=1,onClick}:{children:React.ReactNode;className?:string;level?:1|2|3|4;onClick?:()=>void}){return <div className={`glass glass-${level} ${className}`} onClick={onClick}>{children}</div>}
export function GlassButton({children,className='',variant='secondary',onClick,type='button',disabled=false,ariaLabel}:{children:React.ReactNode;className?:string;variant?:'primary'|'secondary'|'ghost'|'danger';onClick?:()=>void;type?:'button'|'submit';disabled?:boolean;ariaLabel?:string}){return <button type={type} className={`glass-btn glass-btn-${variant} ${className}`} onClick={onClick} disabled={disabled} aria-label={ariaLabel}>{children}</button>}
export function Chip({children,active=false,color,className='',onClick}:{children:React.ReactNode;active?:boolean;color?:string;className?:string;onClick?:()=>void}){return <button className={`chip ${active?'chip-active':''} ${className}`} onClick={onClick}>{color&&<span className="chip-dot" style={{background:color}}/>}{children}</button>}
